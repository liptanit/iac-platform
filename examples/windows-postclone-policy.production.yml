---
# Production baseline currently approved by Payong on 2026-07-09.

windows_rdp_enabled: true

windows_domain_join_enabled: false
windows_domain_name: ""
windows_domain_ou_path: ""

windows_automation_admin_name: ""
# Set ANSIBLE_AUTOMATION_ADMIN_PASSWORD in a root-only env file before enabling.

windows_zabbix_agent_enabled: true
windows_zabbix_agent_install_method: "script"
windows_zabbix_agent_install_script_url: "http://10.1.0.15/agent/windows/Install-ZabbixWindowsAgent.ps1"
windows_zabbix_server: "10.1.0.15"
windows_zabbix_host_metadata: "Windows"
windows_zabbix_force_reinstall: false
windows_zabbix_agent_service_name: "Zabbix Agent 2"

# Trend AI Deep Security / Vision One Endpoint Security can be enabled after
# the approved installer command, token, and service name are supplied.
windows_edr_enabled: false
windows_edr_install_command: ""
windows_edr_service_name: ""

windows_update_policy_enabled: true
windows_update_au_options: 4
windows_update_scheduled_install_day: 0
windows_update_scheduled_install_time: 3

windows_firewall_enabled: true
windows_enable_icmp: true
windows_audit_policy_enabled: true

# Phase 51: SQL Server role automation.
# Applies only to inventory hosts with `role = "sql-server"`.
# Set ANSIBLE_SQL_INSTALL_SOURCE to a path containing setup.exe before enabling
# full SQL install. Without media, the baseline prepares directories/firewall and
# reports SQL as Missing without failing the Windows rollout.
windows_sql_enabled: true
windows_sql_install_source: "{{ lookup('env', 'ANSIBLE_SQL_INSTALL_SOURCE') }}"
windows_sql_fail_when_source_missing: false
windows_sql_instance_name: MSSQLSERVER
windows_sql_collation: Thai_CI_AS
windows_sql_max_memory_mb: 53248
windows_sql_port: 1433
windows_sql_firewall_remote_addresses: []
